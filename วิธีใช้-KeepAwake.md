# KeepAwake — กันไม่ให้ Mac หลับ

แอปหน้าต่างเล็กๆ กดเลือกเวลาแล้ว Mac จะไม่หลับ เหมาะกับตอนปล่อยให้ Claude หรืองานอื่นทำงานทิ้งไว้
ใช้ IOKit power assertion ของ macOS โดยตรง — ปิดแอปเมื่อไหร่ ระบบปล่อยให้เครื่องหลับได้อัตโนมัติ

หมายเหตุ: เดิมทำเป็นไอคอนบนแถบเมนู แต่เครื่องคุณเป็น MacBook จอมีรอยบาก (notch)
และมีไอคอนบนแถบเมนูเต็มแล้ว ระบบจึงไม่มีที่ให้ไอคอนใหม่ จึงเปลี่ยนมาเป็นหน้าต่างควบคุมแทน
ซึ่งแสดงผลได้ชัวร์กว่า

## สถานะตอนนี้

แอปถูกติดตั้งและเปิดให้แล้วที่ ~/Applications/KeepAwake.app — ใช้งานได้เลย

## วิธีใช้งาน

1. เปิดแอป KeepAwake (จาก Launchpad หรือ Spotlight: กด Command+Space พิมพ์ KeepAwake)
2. ในหน้าต่าง เลือกอย่างใดอย่างหนึ่ง:
   - ปุ่ม 30 นาที / 1 / 2 / 4 ชั่วโมง
   - ปุ่ม +15 / +30 นาที — ต่อเวลาเพิ่มจากที่เหลืออยู่ (ถ้ายังไม่เริ่ม จะเริ่มนับเท่านั้น)
   - พิมพ์เวลาในช่องแล้วกด "กำหนดเอง" (เลข = นาที, เติม m = นาที, เติม h = ชั่วโมง เช่น 90, 45m, 2h)
   - "เปิดค้างไว้ (ไม่จับเวลา)" จนกว่าจะกดหยุด
   - "จนกว่างานจะเสร็จ…" = เลือกแอปที่กำลังทำงาน (เช่น Claude) พอแอปนั้นปิด เครื่องกลับมาหลับได้เองอัตโนมัติ ไม่ต้องเดาเวลา
   - ปุ่มสีน้ำเงิน "กัน VPN หลุด — จอไม่ดับ" = กดทีเดียวให้จอไม่ดับและไม่ล็อกจอ เหมาะเวลาต้องคง VPN ไว้

ตัวเลือกเสริม: "หยุดอัตโนมัติเมื่อใช้แบตและต่ำกว่า 15%" — กันลืมถอดปลั๊กแล้วแบตหมดเกลี้ยง
3. ด้านบนจะขึ้นเวลานับถอยหลังแบบสด เช่น "เหลือ 1:59:30"
   และ "เวลาที่เหลือจะโชว์บนไอคอน Dock" ด้วย (เช่น 1:59) — เห็นได้โดยไม่ต้องเปิดหน้าต่าง
4. ครบเวลา Mac กลับมาหลับได้เองตามปกติ (มีแจ้งเตือน)

ไอคอนแอปเป็นรูปถ้วยกาแฟถาวร (เห็นใน Finder, Launchpad, Dock) และจะเปลี่ยนเป็นสีเขียวเมื่อกำลังทำงาน
หมายเหตุ: ถ้าหลังติดตั้ง Dock ยังโชว์ไอคอนเก่า ให้รอสักครู่หรือสั่ง killall Dock หนึ่งครั้ง (ระบบจะรีเฟรชไอคอน)

## ตัวเลือกจอดับ/จอติด

- ติ๊ก "ให้จอดับได้ระหว่างทำงาน" (ค่าเริ่มต้น) = จอดับได้เพื่อประหยัดไฟ แต่เครื่องยังทำงานต่อ
- เอาติ๊กออก = จอติดสว่างค้างด้วย (เผื่อต้องดูหน้าจอตลอด)
- สลับได้แม้กำลังทำงานอยู่ ระบบจะปรับให้ทันทีโดยคงเวลาที่เหลือ

## ทำไม VPN หลุดตอนพักจอ (และวิธีแก้)

โหมด "จอดับได้" (ค่าเริ่มต้น) จะกันแค่ "เครื่อง" ไม่หลับ แต่ "จอ" ยังดับตามเวลาที่ตั้งไว้
พอจอดับ ระบบจะล็อกจอ ซึ่ง VPN บางตัว (เช่น FortiClient) จะตัดการเชื่อมต่อตอนล็อกจอ

แก้โดย: กดปุ่มสีน้ำเงิน "กัน VPN หลุด — จอไม่ดับ" หรือเอาติ๊ก "ให้จอดับได้" ออกแล้วเลือกเวลา
จอจะไม่ดับ ไม่ล็อก VPN จึงอยู่ต่อ

ถ้ายังหลุดอีกทั้งที่จอไม่ดับ แสดงว่า VPN มีตั้งเวลา idle-timeout ของตัวเอง (ต้องไปปรับที่ตัว VPN)

## ประหยัดไฟตอนเปิดค้างไว้ (จอดับ/หรี่ แต่เครื่องทำงานต่อ)

เวลาเปิดค้างไว้ทั้งคืน ไม่จำเป็นต้องเปิดจอสว่างตลอด มี 2 ตัวเลือก:

- ติ๊ก "หรี่จออัตโนมัติเมื่อไม่ได้ใช้ 5 นาที" — ไม่แตะเครื่อง 5 นาทีจะหรี่จอเหลือ 5%
  พอขยับเมาส์หรือพิมพ์ จะคืนความสว่างเดิมให้อัตโนมัติ (จอไม่ดับ จึงไม่ล็อก VPN ไม่หลุด)
- ปุ่ม "ดับจอเดี๋ยวนี้ (เครื่องทำงานต่อ)" — ดับจอทันที เครื่องยังทำงานเต็มที่ ประหยัดไฟสุด
  กดปุ่มใดๆ หรือขยับเมาส์เพื่อให้จอกลับมา

ข้อควรรู้เรื่อง VPN: ตอนนี้เครื่องคุณตั้ง "ล็อกจอทันที" เมื่อจอดับ ดังนั้นการ "ดับจอ" จะล็อกจอ
และ VPN อาจหลุด ถ้าอยากดับจอโดย VPN ไม่หลุด ให้ไปตั้งหน่วงเวลาล็อกจอก่อนที่
System Settings > Lock Screen > "Require password after screen saver begins or display is turned off"
แล้วเลือกเป็นเวลาที่นานขึ้น (เช่น 1 ชั่วโมง) — ต้องตั้งเองเพราะเป็นค่าความปลอดภัยของเครื่อง

ถ้าไม่อยากยุ่งกับค่าความปลอดภัย ให้ใช้ "หรี่จออัตโนมัติ" แทน ปลอดภัยกว่าและ VPN ไม่หลุด

## ปิดหน้าต่าง = ทำงานต่อเบื้องหลัง

ปิดหน้าต่าง (ปุ่มแดง) แอปจะ "ไม่ปิด" แต่ทำงานต่อเบื้องหลัง — VPN/ตัวจับเวลาไม่หลุด
ดูเวลาที่เหลือได้จาก badge บนไอคอน Dock และคลิกไอคอนใน Dock เพื่อเปิดหน้าต่างกลับมา

## หยุด / ออกจากโปรแกรม

- กดปุ่ม "หยุด" ในหน้าต่าง — หยุดกันเครื่องหลับ แต่แอปยังเปิดอยู่
- ออกจากโปรแกรมจริง: กด Command+Q หรือคลิกขวาไอคอนใน Dock > Quit
  (ออกจากโปรแกรมแล้ว Mac กลับมาหลับได้ตามปกติทันที)

## ติดตั้งใหม่ / ย้ายเครื่อง

ถ้าต้องติดตั้งใหม่ ให้วางไฟล์ `KeepAwake.swift` กับ `ติดตั้ง KeepAwake.command`
ไว้โฟลเดอร์เดียวกัน แล้วดับเบิลคลิก `ติดตั้ง KeepAwake.command`
(ใช้ Swift compiler ที่มากับ Command Line Tools ของ Mac — ถ้ายังไม่มี ระบบจะบอกให้รัน xcode-select --install)

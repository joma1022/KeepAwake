# Smoke Test — KeepAwake

รายการกดจริงหลังแก้โค้ดทุกครั้ง ใช้เวลา ~3 นาที
กติกา: ข้อไหนไม่ผ่าน = หยุด แก้ก่อน อย่าไปต่อ | เจอบั๊กใหม่ = เพิ่มข้อใหม่ท้ายลิสต์เสมอ

## ก่อนเริ่ม

- [ ] รัน `รันเทสต์.command` — ต้องผ่านทั้งหมด
- [ ] Terminal: `pmset -g assertions | grep KeepAwake` ต้องว่าง (ยังไม่เริ่ม = ไม่มี assertion)

## วงจรพื้นฐาน

- [ ] เปิดแอป → หน้าต่างขึ้น สถานะ "พร้อมใช้งาน" ปุ่มหยุดกดไม่ได้
- [ ] กด "30 นาที" → นับถอยหลังเดิน การ์ดเป็นสีเขียว ปุ่มหยุดแดง มีแจ้งเตือน 1 ครั้ง
- [ ] Terminal: `pmset -g assertions | grep KeepAwake` ต้องเห็น assertion (ของจริง ไม่ใช่แค่ UI)
- [ ] Dock badge แสดงเวลาที่เหลือ
- [ ] กด "หยุด" → กลับเป็น "พร้อมใช้งาน" + assertion หายจาก pmset

## ต่อเวลา / สลับโหมด (จุดที่เคยพังบ่อยสุด)

- [ ] เริ่ม 30 นาที → กด "+15 นาที" → เวลาเพิ่มเป็น ~45 และ "ไม่มี" แจ้งเตือน "เริ่มแล้ว" ซ้ำ
- [ ] ระหว่างนับถอยหลัง สลับติ๊ก "ให้จอดับได้" ไปมา → เวลาที่เหลือคงเดิม ไม่รีเซ็ต ไม่มีแจ้งเตือนซ้ำ
- [ ] เริ่ม "เปิดค้างไว้" → กด +15 → มีแจ้งเตือนบอกว่าไม่ต้องต่อเวลา

## เฝ้าดูแอป

- [ ] เปิด TextEdit ไว้ → กด "จนกว่างานจะเสร็จ…" เลือก TextEdit → สถานะ "เฝ้าดูงาน" badge 👁
- [ ] ระหว่างเฝ้าดู สลับติ๊กจอ → ยังเป็น "เฝ้าดูงาน" อยู่ (watch ไม่หลุด)
- [ ] ปิด TextEdit → ภายใน ~2 วิ สถานะกลับ "พร้อมใช้งาน" + แจ้งเตือน + assertion หาย

## เบื้องหลัง / ปิดเปิด

- [ ] เริ่มจับเวลา → ปิดหน้าต่าง (ปุ่มแดง) → นับถอยหลังยังเดิน (ดู badge บน Dock)
- [ ] คลิกไอคอน Dock → หน้าต่างกลับมา เวลาตรง
- [ ] Command+Q → แอปปิด + `pmset -g assertions | grep KeepAwake` ว่าง (ไม่มีค้าง!)

## ครบเวลา

- [ ] ตั้ง "กำหนดเอง" = 1 (นาที) รอครบ → แจ้งเตือน + สถานะกลับ + assertion หาย

## จำค่า

- [ ] ติ๊ก/พิมพ์ค่าต่างๆ → Command+Q → เปิดใหม่ → ค่าเดิมอยู่ครบ

## ประวัติบั๊กที่เคยเจอ (ห้ามกลับมา)

1. caffeinate กำพร้าค้าง 57 ชม. → ตรวจข้อ Command+Q ด้านบน
2. UI เขียวแต่ไม่มี assertion จริง → ตรวจด้วย pmset ทุกครั้ง
3. หรี่จอแล้วค้างมืด → เปิดหรี่จอ รอหรี่ ขยับเมาส์ ความสว่างต้องคืน
4. watch หลุดตอนสลับติ๊กจอ → ข้อในหมวดเฝ้าดูแอป
5. แจ้งเตือนสแปมตอนต่อเวลา/สลับโหมด → หมวดต่อเวลา
